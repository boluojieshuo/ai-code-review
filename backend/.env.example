# ===== 服务端环境变量（仅供运维配置，用户无需关心） =====

# 通义千问 API Key（必填）
DASHSCOPE_API_KEY=your-api-key-here

# 模型选择（可选）
LLM_MODEL=qwen-plus

# 数据库连接（可选，留空使用本地 SQLite）
DATABASE_URL=

# JWT 密钥（可选，生产环境请随机生成）
SECRET_KEY=change-me-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
